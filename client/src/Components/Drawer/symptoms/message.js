import { defineMessages } from "react-intl";

const messages = defineMessages({
    symptoms: {
        id: "app.symptoms.symptoms",
        description: "",
        defaultMessage: "Symptoms"
    },
   
});

export default messages;
